spring:
  datasource:
    url: jdbc:h2:mem:studyspace-test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
studyspace:
  auth:
    require-email-verification: false
